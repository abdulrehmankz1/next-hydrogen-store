# Shopify Store Configuration
# Replace with your actual Shopify store domain (e.g., your-store.myshopify.com)
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com

# Storefront Access Token
# Obtain this from your Shopify admin under Online Store > Themes > Customize > App embeds or via API
SHOPIFY_STOREFRONT_ACCESS_TOKEN=your_storefront_access_token_here

# Admin Access Token (if needed for admin API calls)
# Obtain this from Shopify admin under Apps > Develop apps > Create app
SHOPIFY_ADMIN_ACCESS_TOKEN=your_admin_access_token_here

# Session Secret (for session management)
# Generate a random string for production
SESSION_SECRET=your_random_session_secret_here

# Other optional variables
# NODE_ENV is automatically set by Vite, but you can override if needed
# NODE_ENV=development

# Custom variables (prefix with VITE_ to expose to client-side code)
# VITE_CUSTOM_VAR=example_value
